# tasks.md — bug/feature check-in

> Status of the 8 items reported from the other-PC run. Updated **2026-07-04** (Learning tab shipped).
> Legend: ✅ done · 🟡 partial · ❌ open

| # | Item | Status | Detail |
|---|------|--------|--------|
| 1 | Groups: every ticker links to its chart; charts-tab "view chart" must open *that* ticker's chart | ✅ | **Done 2026-07-05.** Every remaining plain-text ticker now links to its own chart/overview: Charts-tab chart cards, rankings-table "Top Stock" column, group-detail "Top Stock" card, and the constituent-stocks table. |
| 2 | Acronym hover bubbles (full name + 1-sentence summary, long-hover, semi-transparent) | ✅ | **Done 2026-07-07.** Ticker hover bubbles now come from the shared ticker components and cover scan rows, group picks, digest/watchlists, stock detail peers, validation events, Backplay top-10 rows, theme constituents, daily snapshot cards, key markets, and static daily snapshot. Acronym bubbles keep the 700 ms semi-transparent behavior and now cover Breadth/Backplay/Learning terminology gaps, including 5D/10D breadth labels. |
| 3 | Groups → selected group: top-stock chart link; charts show hover info, **+** watchlist, link to single-ticker overview | ✅ | **Done 2026-07-05.** Overview top-stock links to the ticker page; Charts-tab cards now show hover info on the ticker (last price, period % change), a **+** add-to-watchlist button, and the ticker links to the single-ticker overview; constituent table rows also got the **+**. |
| 4 | ~~missing_theme_links warning~~ | — | Dropped per your note ("ignore"). |
| 5 | Theme discovery model options: local models, Hermes, Codex (saved auth) — all options | ✅ | **Done 2026-07-05, verified live.** The Themes model picker now offers: **your local Ollama models** (all 15 enumerated live — gemma4, qwen3, gpt-oss, RavenX-Trade, …), **Hermes gateway** (= whatever the assistant picker runs: Claude Pro / **Codex via ChatGPT Plus** — auth saved once in Hermes via LOGIN-CHATGPT.bat, then remembered in `auth.json`), and **Groq** (your env key). Minimax/Z.AI removed from the options and from the default chain ("cloud provider unknown" gone — defaults now resolve to Groq). Selection persists in the DB and routes extraction/merge/IBD accordingly. 41 backend tests pass. |
| 6 | Learning tab: LIVE_APP_GUIDE.md as HTML + full acronym terminology w/ hover, aimed at a 1-trade-a-day trader | ✅ | **Done 2026-07-04.** New **Learning** nav tab (`/learning`), two views: **App Guide** — LIVE_APP_GUIDE.md (recovered from `.original-repo`, now in `docs/`) rendered as HTML with tables, screenshots, and acronym hover bubbles throughout; **Terminology** — every glossary term with full name, what it tells you, where it's used, and how it helps a 1-trade-a-day trader. Verified serving in Docker (`/learning` → 200); 483/483 frontend tests pass. |
| 7 | Backtest UX improvements for a beginner | ❌ | Backtest tab (`/validation`) exists, but no beginner-focused UX work has been done. Needs a spec pass: what confused you first? |  
there is nothing to execute any of it, there is just a list of recent tickers, but no way to backtest.  no obvious way to initialize backtest from other areas that I can tell.  I want 2 types of backtests, i want the original repo creators backtest platform, but working, and i want additional of my own, called backplay.  3 options in backplay:1. it should mimic tradingview backtest play button feature.  2.  It should combine a scan preset (custom and pre) find and run backtests on 1st 10 with highest score. In other workds, it shouldnt just backtest a strategy on a stock, it should find several stocks that fit a strategy, and run the backtest on them 3.  it should run a backtest on a single user defined stock on date range. If you have questions on overview of what I want, keep trading terms out of it, I'm not well learnt on it yet.  4.  I would like a short term paper trader.  I want to use scripting similar or exactly like thinkorswim in order to set up a trade, and a scan2trade combo, and see if it wins or loses.  It should be ongoing until I stop, it should be logged in similar row fashion, and on click open up to more info.
| 8 | Assistant returns "No assistant response returned." while Hermes shows green | ✅ | **Fixed 2026-07-05, verified end-to-end** (assistant answered a live test prompt through backend → Hermes → local Ollama `gemma4`). Three stacked faults repaired: Hermes container crash-looping (compose `user:` flag the image forbids — removed, now uses `HERMES_UID/GID`); `HERMES_API_KEY` missing from root `.env` (synced); Hermes only starts with the assistant profile — use `docker compose --profile assistant up -d` (the CJN launcher scripts already do). |

## Done since the other-PC run (context)
- ✅ Docker "every tab couldn't load" — root-caused (backend lost boot race vs Postgres recovery) and fixed with `wait_for_database` retry + tests; stack verified healthy.
- ✅ News Council engine (FOSS news + citations + 7-day ML/backtest digest) built + unit-tested; not yet in UI.

## Next check-in
Items will flip to ✅ here as they land. Open questions for you:
1. **#7** — one or two examples of what felt confusing in Backtest would set the spec.

Remaining open: **#7 only** — (a) make the original Backtest platform executable from the UI (today it just lists recent signals, nothing to run), and (b) **Backplay** with 3 modes: TradingView-style play button, scan-preset top-10 auto-backtest, single-ticker + date-range. In progress next.
